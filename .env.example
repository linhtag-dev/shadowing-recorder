# Runtime settings for the Node.js service. The browser accepts YouTube URLs
# locally and does not need a video configuration value or credential.
HOST=127.0.0.1
PORT=3000

# The default resolves to apps/web/dist. Override only when serving a build
# from another location.
# WEB_DIST_PATH=/absolute/path/to/apps/web/dist
